# sources/

Drop **your own** exports here (LinkedIn, X, newsletters, etc.) — **do not commit private data** to a public fork.

Supported extensions in ingest: `.md`, `.csv`, `.txt` (see `scripts/build-ingest-payload.cjs`).

For a public template repo, keep this folder empty or use synthetic demo files only.
